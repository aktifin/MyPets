# MyPets 多人云养桌面宠物实施计划

更新基线：`main` 已合并 Web 用户门户、内容治理、D3 部署管理台、数据库迁移、撤销素材回执和系统托盘优化。本计划只记录已由代码和测试确认的能力。

## 一、产品与技术边界

- 服务端是宠物状态、关系、提醒、消息、素材版本、版权档案和撤销回执的权威来源。
- 桌面 SQLite 仅作为可重建缓存和断点续办队列。
- 宠物素材包只能包含声明式资源和 Manifest，禁止脚本和可执行文件。
- 投稿、制作、证据、复核、发布、回退和撤销必须保留角色分离、状态机和审计。
- AI 仅允许自然聊天、有限记忆和语音交互，不提供工具、文件或设备控制能力。

## 二、当前已实现能力

| 能力域 | 状态 | 当前边界 |
|---|---|---|
| 账户、设备与同步 | 已实现 | JWT、设备凭据、快照、增量事件、多端当前宠物和设备撤销回执 |
| 多宠物与成长 | 已实现基础闭环 | 等级、经验、成长阶段、羁绊、性格和互动履历 |
| 网页与桌面照料 | 已实现 | 投喂、玩耍、清洁、摸摸、休息、冷却和日限额 |
| 好友、共同照料与串门 | 已实现 | 好友、屏蔽、隐私、caregiver/viewer、异步串门和双宠展示 |
| 消息与提醒 | 已实现基础闭环 | 分类会话、跨端已读、MyReminder 导入、完成、贪睡和恢复摘要 |
| 系统托盘与快捷操作 | 已实现 | 分组菜单、动态状态、未读消息、宠物切换、四向吸附和云端操作 |
| Web 用户门户 | 已实现 Phase1 | Dashboard、宠物、好友、串门、消息、提醒、成长、互动和部署状态 |
| 公共与专属素材 | 已实现 | 模板版本、ZIP 校验、不可变 Release、稳定通道、单宠部署和回退 |
| 用户原图与制作工单 | 已实现 | 图片安全处理、审核、工单、补图、失败重试、产物校验和日志 |
| 版权治理 | 已实现增强闭环 | 证据附件、有效期、独立复核、复核意见、历史、发布门禁和撤销分发 |
| 管理员 RBAC | 已实现 | editor/reviewer/publisher/auditor/superadmin 页面与服务端分权 |
| 数据库迁移 | 已实现增量链 | `001`、`002`、`003`、38 张业务表、既有库接管和双数据库 CI |
| 测试与 CI | 持续运行 | Linux、Windows、SQLite、PostgreSQL、Web JavaScript 和 Qt 冒烟 |

## 三、已完成开发批次

### 内容治理与 D3 部署

- [x] 普通账户不得调用管理端治理接口。
- [x] editor、reviewer、publisher 和 auditor 职责分离。
- [x] 版权声明默认 `pending`，声明人不能自审。
- [x] D3 审核、发布和下载要求最新版权记录处于可用状态。
- [x] 重复发布当前 Release 保持幂等。
- [x] 撤销后停止分发并触发客户端缓存清理。

### 数据库迁移与 PostgreSQL

- [x] `001_initial_schema` 冻结 35 张基础业务表。
- [x] `002_asset_revocation_ack` 增加设备撤销回执表，业务表增至 36 张。
- [x] `003_rights_evidence_history` 增加版权证据、历史和有效期字段，业务表增至 38 张。
- [x] SQLite 和 PostgreSQL 16 完成 head、逐级降级、base 和再升级检查。
- [x] 历史 `create_all` 数据库支持结构比对和安全接管。
- [x] 生产环境禁止启动自动建表。

### 管理台版权证据、有效期与历史

- [x] editor 可登记授权起止时间并上传 PDF、PNG、JPEG 或纯文本证据。
- [x] 已审核的原始投稿图片自动关联为版权证据。
- [x] 单个附件限制 8 MB，按 SHA-256 去重并存入对象存储。
- [x] reviewer 独立复核时保存复核意见、复核人和时间。
- [x] 管理台集中展示证据清单、有效期、复核信息和不可变状态历史。
- [x] 附件下载使用短期管理员令牌和 `private, no-store`。
- [x] 未生效和已过期授权不能通过 D3 审核、发布或继续下载。
- [x] verified/revoked 存证的条款和附件冻结，变更需创建新历史版本。

### 撤销素材客户端清理与设备回执

- [x] `asset_revoked` 在推进同步游标前保存为本地任务。
- [x] 精确清理缓存并写入不可逆撤销标记。
- [x] 当前宠物自动切换到安全兼容形象。
- [x] 失败任务在启动或网络恢复后重试。
- [x] 新设备 Bootstrap 主动同步历史撤销清单。
- [x] 设备按 right/release/device 幂等提交清理回执。

### Windows 系统托盘菜单优化

- [x] 菜单重组为宠物互动、宠物管理、社交提醒健康、桌面行为和云端账户。
- [x] 顶部展示当前宠物、成长等级、归属和在家/串门状态。
- [x] 动态刷新可见性、暂停、连接、未读和吸附方向。
- [x] 云端与本地宠物分组展示并标记兼容形象。
- [x] 支持上下左右四向吸附。

## 四、尚未完成的生产化工作

### A. 数据库、对象存储与部署

- [ ] 完成对象存储生产适配、病毒扫描、备份恢复演练和发布回滚演练。
- [ ] 增加生产部署模板、迁移前检查和维护窗口脚本。
- [ ] 为新增 Revision 建立生产数据量级压测和长事务监控。

### B. 内容运营闭环

- [ ] 增加授权到期前预警和到期自动撤销/缓存清理事件。
- [ ] 在管理台增加设备清理回执聚合、失败设备筛选和人工跟进状态。
- [ ] 对视觉特征一致性引入可解释评分，但不得自动替代人工审核。
- [ ] 增加批量筛选、批量复核和运营统计，同时保留逐项确认。

### C. 核心养宠体验

- [ ] 日常任务、连续照料奖励和长期成长平衡。
- [ ] 多宠批量状态查看、批量切换和统一提醒。
- [ ] 多宠物聚会模型、邀请状态机和多窗口布局。
- [ ] 更丰富的环境事件、主动互动和好友宠物互动记录。

### D. 受限 AI 交互

- [ ] 无工具调用的宠物人格文字聊天。
- [ ] 有限记忆、用户可清除记忆和状态联动。
- [ ] 按键说话、ASR/TTS、字幕和中断控制。
- [ ] 明确禁止常开监听、工具执行和设备控制。

## 五、下一批优先级

1. 管理台设备清理回执聚合、失败筛选和人工跟进。
2. 授权到期预警及到期后的服务端撤销事件闭环。
3. 对象存储生产适配、扫描和备份恢复演练。
4. 多宠物聚会服务端模型与状态机。

## 六、完成判定

任何功能只有同时满足以下条件才标记为完成：

- 服务端状态机或客户端逻辑已实现；
- 权限、幂等、审计和异常路径已覆盖；
- SQLite 与 PostgreSQL 迁移 CI 通过；
- Linux 与 Windows CI 通过；
- Web JavaScript、后端编译和 Qt 冒烟通过；
- 文档与 `main` 中实际代码一致。
