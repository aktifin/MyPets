# MyPets 客户体验优先实施计划

更新基线：`main` 已具备账户、宠物、好友、串门、消息、提醒、素材和治理基础能力。此前开发对后台治理投入过多，但客户侧首次使用、日常养宠和多宠操作仍不顺畅。本计划从 2026 年 7 月起调整为“客户体验优先”。

## 一、产品优先级原则

1. 客户第一次打开后，应在 3 分钟内完成注册或本地体验、领养或选择宠物并完成第一次互动。
2. 日常高频操作必须在首页、宠物点击面板或系统托盘一到两步内完成。
3. 客户页面不得出现模板 ID、版本号、状态机编码等后台技术字段。
4. 页面按“当前最需要处理的任务”组织，不再按后端模块平铺入口。
5. 多宠切换、消息、提醒和串门必须提供统一、明确的状态反馈。
6. 治理、安全和审计功能进入维护冻结；除生产阻断、安全漏洞或法规要求外，不再优先扩展。
7. AI 只保留自然聊天、有限记忆和语音交互，不提供 Agent、工具执行或设备控制。

## 二、当前客户侧能力评估

| 能力域 | 当前状态 | 主要问题或边界 |
|---|---|---|
| 注册与登录 | 已实现 | Web 引导已衔接；桌面可从首次引导进入云端登录 |
| 首次领养 | 已完成 Web 重构 | 客户只选择形象、名字和性格，技术版本由系统处理 |
| 养宠首页 | 已完成第九阶段 | 已有推荐照料、每日任务、连续陪伴、主动关怀、成长目标、统一待办和多宠总览 |
| 宠物切换 | 已完成第三阶段 | Web、桌面和托盘支持轮换，照料完成后可直接进入下一只需关注宠物 |
| 桌面首次运行 | 已完成第一阶段 | 本地体验、云端登录和基础操作说明已串联 |
| 桌面点击交互 | 已完成第四阶段 | 已展示状态、任务、连续天数、冷却、主动提示和下一成长目标 |
| 日常照料 | 已完成第五阶段 | 跨端任务、主动提示、成长反馈和多宠照料闭环已衔接 |
| 成长反馈 | 已完成第一阶段 | 已有下一等级、下一阶段、羁绊进度和成长纪念册 |
| 主动关怀 | 已完成第二阶段 | 单宠与多宠提示均支持聚合、免打扰、频率限制和用户关闭 |
| 待处理事项 | 已完成第三阶段 | 好友、共同照料、串门和到期提醒已统一，可直接处理、进入详情并回看结果 |
| 处理记录 | 已完成第一阶段 | Web 与 Windows 支持类型、时间筛选和原详情回跳 |
| 多宠管理 | 已完成第二阶段 | 已有状态排序、任务总览、轮换、聚合提醒和双宠桌面布局 |
| 串门 | 已完成第二阶段 | Web 与桌面可按一条时间线查看申请、接受、到达、互动和返家 |
| 多宠物聚会 | 已完成最小可用版本 | 每场最多四只、每账户一只；所有成员进入单一聚会面板，桌面常驻宠物仍最多两只 |
| 消息 | 已完成第三阶段 | 分类、搜索、未读定位、详情跳转和账户级快捷回复均可用 |
| 系统托盘 | 已优化 | 已具备状态、切换、待办数量、处理记录、多宠轮换、双宠布局、主动关怀、四向吸附和云端操作 |
| 后台治理 | 能力充分 | 暂停非阻断性扩展，只做缺陷修复和必要维护 |

## 三、已完成：Web 首次养宠与首页重构

- [x] 客户可理解的宠物形象目录自动映射内部模板和版本。
- [x] 没有运营素材时提供内置起始形象。
- [x] 注册或登录后的空账户自动进入首次领养向导。
- [x] 领养流程缩减为选择形象、名字和性格。
- [x] 客户侧隐藏模板 ID 和内部版本字段。
- [x] 顶栏支持宠物切换和添加宠物。
- [x] 首页按当前状态推荐一次合理照料动作。
- [x] 串门期间不再错误推荐本机照料。

## 四、已完成：桌面首次运行与主交互重构

- [x] 三步首次运行引导支持本地体验或云端登录。
- [x] 点击宠物后展示通俗状态、成长、羁绊和位置。
- [x] 五类高频照料可从快捷面板一步完成。
- [x] 串门、离线和无权限状态提前阻断并解释原因。
- [x] 本地体验宠物支持真实本机照料。
- [x] 云端照料展示状态和经验变化。
- [x] 修复顶部和底部吸附设置恢复问题。

## 五、已完成：每日任务、连续陪伴与冷却解释

- [x] 基于既有照料事件生成服务端每日摘要，不新增治理表。
- [x] 每日任务统一为 3 次照料、2 种方式和 1 次陪伴。
- [x] Web、云端桌面和本地体验宠物使用一致规则。
- [x] 当天未结束时不提前中断连续陪伴记录。
- [x] 完成全部任务后点亮非付费、不可交易的陪伴徽章。
- [x] 冷却、每日上限、串门、离线和只读关系在操作前解释。

## 六、已完成：适度主动关怀与免打扰

- [x] 低状态、连续 12 小时未互动和到期提醒可生成通俗提示。
- [x] 每次只选择一条优先提示，避免多个弹窗。
- [x] 云端偏好和处理结果复用同步事件，不新增数据库表。
- [x] 支持分类开关、免打扰、最小间隔、每日上限和稍后提醒。
- [x] Web、桌面和托盘均可处理提示。
- [x] 文案不使用惩罚、损失或强迫性连续打卡表达。

## 七、已完成：成长目标与纪念记录

- [x] 复用现有成长、羁绊和阶段阈值，不建立第二套体系。
- [x] 展示下一等级、下一阶段目标和剩余经验。
- [x] 展示成长与羁绊等级内进度。
- [x] 升级、羁绊提升和阶段变化形成纪念记录。
- [x] Web 首页、宠物详情和桌面状态面板均可查看。
- [x] 本地体验宠物与云端使用一致经验增量和阶段阈值。

## 八、已完成：统一待处理事项

- [x] 好友申请、共同照料邀请、串门申请和到期提醒整合为一个账户级队列。
- [x] 队列直接投影现有业务数据，不增加待办表或第二套状态机。
- [x] 高优先级到期提醒置顶，其余按发生或到期时间排序。
- [x] 每条事项显示对象、宠物、发生或到期时间、主操作和次操作。
- [x] 好友、共同照料和串门支持直接接受或拒绝。
- [x] 提醒支持直接完成、10 分钟后提醒或忽略。
- [x] 所有直接操作继续调用原有权限和生命周期规则。
- [x] Web 首页展示统一列表、数量和优先事项数量。
- [x] 桌面提供独立待办中心，系统托盘主菜单显示实时数量。
- [x] 处理成功后立即移出列表，并同步 Web 和其他桌面设备。
- [x] 账户令牌与设备令牌读取同一队列并执行相同权限校验。

## 九、已完成：多宠状态总览与轮换照料

- [x] 账户和设备可一次读取全部可访问宠物的状态、关系、位置和今日任务摘要。
- [x] 排序遵循“真实状态问题优先、日常任务其次、稳定宠物最后”。
- [x] 状态分为优先关注、需要照料、今日陪伴、状态良好和暂不可照料。
- [x] 串门、只读关系、动作冷却和每日上限继续阻断直接照料并解释原因。
- [x] 健康需要查看但无对应直接动作时，只切换到宠物状态页。
- [x] Web 首页新增多宠卡片、任务进度、切换入口和可用推荐动作。
- [x] 桌面合并本地体验宠物和云端宠物，并提供下一只需关注快捷切换。
- [x] 不提供批量自动照料，所有具体动作仍由用户逐项确认。

## 十、已完成：多宠关怀闭环与双宠桌面布局

- [x] 多只宠物同时需要关注时只生成一条聚合摘要。
- [x] 聚合摘要继续遵守免打扰、最小间隔、每日上限和跨端频率限制。
- [x] 照料完成后，Web 和桌面提示下一只真正需要关注的宠物。
- [x] 后续提示只提供“稍后”或显式切换，不自动切换、不自动照料。
- [x] Windows 桌面同时展示当前宠物与一只轻量伴随宠物。
- [x] 双宠位置和伴随宠物选择按设备持久化，支持恢复和重新排列。
- [x] 双宠模式启用时解除边缘吸附；双击第二只可切换为当前宠物。

## 十一、已完成：串门时间线与消息关联效率

- [x] 串门申请、接受、到达、双宠互动、返家和异常结束投影为一条客户时间线。
- [x] 时间线读取现有 `PetVisit` 时间戳和互动同步事件，不新增历史表。
- [x] 只有申请方和接待方可读取，互动记录按账户事件流隔离并去重。
- [x] Web 和 Windows 消息可识别相关好友、宠物或串门并进入详情。
- [x] Web 待办可进入好友、宠物、串门或提醒详情；Windows 复用现有窗口。

## 十二、已完成：客户处理历史

- [x] 好友申请、共同照料和串门结果直接读取原业务状态及响应时间。
- [x] 提醒完成、稍后提醒和忽略从账户同步事件恢复，可保留多次贪睡记录。
- [x] 旧终态提醒使用原完成或忽略时间兼容展示，且不与事件记录重复。
- [x] Web 与 Windows 支持按业务类型和 7 天、30 天、90 天、一年、全部范围筛选。
- [x] 每条记录均可重新打开好友、共同照料、提醒或串门时间线详情。
- [x] 账户令牌与设备令牌只能读取本账户记录，未新增历史表或本地重复存储。

## 十三、已完成：消息检索、未读定位与可配置快捷回复

- [x] Web 和 Windows 支持按联系人、宠物、会话标题和消息内容搜索。
- [x] 搜索结果可定位匹配的历史消息，而不是只打开会话底部。
- [x] 长会话按目标消息加载前后窗口，不再固定只读取最早 100 条。
- [x] 打开会话不再自动清空全部未读消息。
- [x] 支持第一条、上一条和下一条未读导航，并高亮当前目标。
- [x] 只有用户点击“读到这里”时才推进原有 `last_read_sequence`。
- [x] 快捷回复按普通私聊、好友宠物、串门和共同照料四类配置。
- [x] 每类支持 1 至 6 条、保持用户排序、去重并可恢复本类或全部默认。
- [x] 配置通过账户同步事件在 Web 与绑定桌面设备间共享，不新增设置表。
- [x] 点击快捷回复只填入输入框，必须由用户再次确认发送，不进行内容分析或自动代发。

## 十四、已完成：多宠物聚会最小可用版本

- [x] 建立聚会创建、逐人邀请、接受、谢绝、开始、离场、取消、主动结束和到期自动结束状态。
- [x] 每场最多四只宠物，每个参与账户只允许携带一只自己管理的宠物。
- [x] 聚会开始前重新校验宠物位置、串门占用和其他聚会占用，避免同一宠物重复参加。
- [x] 提供围圈打招呼、一起玩耍、留下合影记录和一起休息四类预设互动。
- [x] 聚会成员、互动、离场和结束过程形成统一客户时间线。
- [x] 互动复用账户级同步事件，不新增互动历史表、第二同步游标或本地重复存储。
- [x] 聚会过程不创建或修改宠物归属、共同主人、照料者和观察者关系。
- [x] Web 将全部成员呈现在同一个聚会场景面板，不创建额外浏览器窗口或后台 Worker。
- [x] Windows 仅新增一个非模态聚会管理对话框，不创建额外宠物运行时、同步实例或本地数据库。
- [x] 桌面常驻宠物继续严格限制为“当前宠物 + 一只轻量伴随宠物”，上限两只。
- [x] 到期聚会沿用惰性结算，在下一次聚会、宠物快照或照料请求时恢复宠物在家状态，不增加常驻调度器。

### P2：受限 AI 交互

- [ ] 无工具调用的宠物人格文字聊天。
- [ ] 有限记忆、用户可查看和清除记忆。
- [ ] 状态、性格和成长阶段影响回复风格，但不替用户执行操作。
- [ ] 按键说话、ASR/TTS、字幕和中断控制。

## 十五、暂停扩展的后台方向

以下事项不再作为近期优先级：

- 新增治理统计、负责人分派和更多审计维度；
- 视觉身份自动评分和批量复核；
- 版权到期运营报表；
- 非生产阻断性的数据库和对象存储增强。

现有权限、发布门禁、撤销清理和设备回执继续保留并维护，但不得挤占客户侧功能开发资源。

## 十六、下一批优先级

1. 对多宠物聚会开展 Web、Windows 和多账户实际交互验收，只修复入口、空状态、反馈和布局类问题。
2. 评估将聚会邀请以只读投影方式接入统一待处理事项和托盘数量，不新增待办表或第二套邀请状态机。
3. 持续保持每场最多四只、每账户一只、桌面常驻最多两只的边界，不扩展多人自由房间、临时照料权限或多宠物窗口实例。
4. 在不接入 Agent、工具执行和设备控制的前提下，准备宠物人格文字聊天与用户可清除的有限记忆。

## 十七、客户体验完成判定

客户侧功能只有同时满足以下条件才算完成：

- 普通用户不阅读说明即可理解主要入口；
- 高频任务不超过两次点击或一次托盘操作；
- 不展示后台编码、内部版本和技术状态；
- 空状态、加载、成功、失败、冷却和无权限均有明确反馈；
- 手机、桌面浏览器和 Windows 桌面端布局可用；
- 自动化测试覆盖主流程，但测试通过不能替代实际交互逻辑审查；
- 文档、页面文案和 `main` 中实际行为一致。
