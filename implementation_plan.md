# MyPets 多人云养桌面宠物实施计划

更新基线：`main` 已合并 Web 用户门户 Phase1（PR #28）。本计划只记录已由代码和测试确认的能力，不再混用历史分支状态。

## 一、产品与技术边界

- 服务端是宠物状态、关系、提醒、消息和素材版本的权威来源。
- 桌面 SQLite 仅作为可重建缓存，不作为跨端最终事实。
- 宠物素材包只能包含声明式资源和 Manifest，禁止脚本、可执行文件和工具调用。
- 投稿、制作、审核、发布、回退和版权撤销必须保留角色分离、状态机和审计记录。
- AI 仅允许自然聊天、有限记忆和语音交互，不提供 Agent、Shell、文件、浏览器或设备控制能力。

## 二、当前已实现能力

| 能力域 | 状态 | 当前边界 |
|---|---|---|
| 账户、设备与同步 | 已实现 | JWT、设备凭据、快照、增量事件、多端当前宠物 |
| 多宠物与成长 | 已实现基础闭环 | 等级、经验、成长阶段、羁绊、性格和互动履历 |
| 网页与桌面照料 | 已实现 | 投喂、玩耍、清洁、摸摸、休息、冷却和日限额 |
| 好友与共同照料 | 已实现 | 好友申请、屏蔽、隐私、caregiver/viewer 邀请 |
| 异步串门 | 已实现 | 申请、接受、拒绝、取消、召回、到期返家和双宠展示 |
| 消息与已读 | 已实现 | 私聊、宠物身份、业务消息投影、分类会话和跨端已读 |
| MyReminder | 已实现基础闭环 | 提醒导入、快照、投递、完成、贪睡、忽略和恢复摘要 |
| Web 用户门户 Phase1 | 已实现 | Dashboard、宠物、好友、串门、消息、提醒、成长、互动和部署状态 |
| 公共素材发布 | 已实现 | 模板版本、ZIP 校验、审核、不可变 Release、稳定通道和回滚 |
| 用户原图投稿 | 已实现 | 图片解码、安全清理、权利声明、审核和鉴权下载 |
| 素材制作工单 | 已实现 | 分配、进度、补图、失败重试、产物校验和日志 |
| 专属素材部署 | 已实现后端闭环 | 独立审核、私有 Release、单宠 active/previous、下载和回退 |
| 视觉身份与版权模型 | 已实现基础模型 | `pet_visual_identities`、`pet_asset_rights` 和撤销事件 |
| 管理员 RBAC | 已实现基础能力 | editor/reviewer/publisher/auditor/superadmin |
| 测试与 CI | 持续运行 | Linux、Windows、后端、Web JavaScript 和 Qt 冒烟 |

## 三、当前开发批次：内容治理安全加固

- [x] 普通账户不得调用 `/api/v1/admin/governance/*`。
- [x] 视觉身份维护归属 editor 权限。
- [x] 版权声明以 `pending` 状态登记，不再由声明人自动完成核验。
- [x] reviewer 独立复核版权存证，禁止声明人自审。
- [x] publisher 执行版权撤销并生成审计和 `asset_revoked` 同步事件。
- [x] 专属素材审核和发布必须存在最新的 `verified` 权利记录。
- [x] 版权撤销或重新待审后停止私有素材包下载。
- [x] 重复发布当前已部署 Release 按幂等读取处理，不再递增 `state_version` 或重复写审计事件。
- [x] 增加角色、权利生命周期、撤销分发和重复发布回归测试。

## 四、尚未完成的生产化工作

### A. 数据库与部署

- [ ] 建立正式 `alembic/versions` 迁移版本链。
- [ ] 从现有 `Base.metadata.create_all` 数据库生成可验证的基线迁移。
- [ ] 完成 SQLite 升级、全新 PostgreSQL 建库和 PostgreSQL 增量升级测试。
- [ ] 生产环境默认关闭 `create_schema_on_start`。
- [ ] 完成对象存储、备份恢复、升级回滚和发布运维文档。

### B. 内容运营闭环

- [ ] 核查并补齐管理台专属素材 D3 审核、发布和版权治理操作界面。
- [ ] 为版权存证增加附件证据、有效期、复核意见和历史版本展示。
- [ ] 增加撤销后部署状态提示、客户端缓存清理确认和安全降级反馈。
- [ ] 对视觉特征一致性引入可解释评分，但不得自动替代人工审核。

### C. 核心养宠体验

- [ ] 日常任务、连续照料奖励和长期成长平衡。
- [ ] 多宠批量状态查看、批量切换和统一提醒。
- [ ] 多宠物聚会模型、邀请状态机和多窗口布局。
- [ ] 更丰富的环境事件、主动互动和好友宠物互动记录。

### D. 受限 AI 交互

- [ ] 无工具调用的宠物人格文字聊天。
- [ ] 有限记忆、用户可清除记忆和状态联动。
- [ ] 按键说话、ASR/TTS、字幕和中断控制。
- [ ] 明确禁止常开监听、工具执行和设备控制。

## 五、下一批优先级

1. 建立 Alembic 基线迁移和 PostgreSQL CI 验证。
2. 补齐管理台版权存证、独立复核、撤销和 D3 发布界面。
3. 完善客户端对 `asset_revoked` 的缓存清理回执和安全降级。
4. 开始多宠物聚会服务端模型与状态机。

## 六、完成判定

任何功能只有同时满足以下条件才标记为完成：

- 服务端状态机或客户端逻辑已实现；
- 权限、幂等、审计和异常路径已覆盖；
- Linux 与 Windows CI 通过；
- Web JavaScript、后端编译和 Qt 冒烟通过；
- 文档描述与 `main` 中实际代码一致。
