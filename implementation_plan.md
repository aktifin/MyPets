# MyPets 多人云养桌面宠物实施计划（功能收敛版）

当前开发目标调整为：优先完善桌面宠物、多宠物、好友互动、提醒和自然聊天体验。宠物素材审核、用户原图投稿、制作工单及专属素材发布目前不属于核心用户路径，相关代码保留但默认不加载。

原则：

- 服务端保持宠物状态权威；
- 桌面 SQLite 仅作为可重建缓存；
- 优先实现直接提升养宠体验的功能；
- 暂不维护宠物内容审核和生产运营体系；
- 已发布宠物素材只读目录继续保留，避免影响现有宠物和客户端缓存。

---

## 一、当前运行能力

| 能力域 | 当前状态 | 说明 |
|---|---|---|
| 账户、设备与同步 | 已实现 | JWT、设备凭据、快照、语义增量事件、多端当前宠物 |
| 多宠物与成长 | 已实现基础闭环 | 五阶段、等级、经验、成长日志、性格积分 |
| 投喂与互动 | 已实现 | 服务端照料规则、冷却、限额、桌面照料入口 |
| 好友与共同照料 | 已实现 | 好友、屏蔽、隐私、共同照料角色与邀请 |
| 异步与桌面串门 | 已实现 | 串门申请、接待、召回、送客、到期返家、双宠互动 |
| 消息与已读同步 | 已实现 | 私聊、宠物身份、业务分类、跨端已读 |
| MyReminder | 已实现基础闭环 | 提醒同步、桌面投递、贪睡、休眠恢复摘要 |
| 桌面边缘吸附 | 已实现 | 上下左右吸附、半隐藏、多显示器恢复 |
| 已发布素材读取 | 保留 | 公共目录查询、哈希校验、客户端下载和本地缓存 |
| 宠物审核与内容运营 | 默认屏蔽 | 投稿、审核、制作工单、专属发布和管理台均不注册路由 |
| Web 用户门户 | 已实现 | 账户、宠物、好友、共同照料和串门；不显示专属形象审核入口 |
| 测试与 CI | 持续运行 | Linux、Windows、后端、Web JavaScript 与 Qt 冒烟 |

### 审核功能屏蔽策略

默认配置：

```text
MYPETS_ENABLE_PET_REVIEW=0
```

关闭时不加载：

- 用户宠物原图提交与查询 API；
- 管理员原图审核 API；
- 素材制作工单及参考图 API；
- 专属素材部署审核、发布、下载和回退 API；
- 模板内容审核、发布、回滚和管理台 API；
- `/admin` 宠物内容管理页面；
- 用户门户“专属形象”页签及相关 JavaScript。

保留但不主动使用：

- 已有数据库表和历史数据；
- 已实现的审核、工单和部署源代码；
- 已发布官方素材的公开只读目录和下载接口。

需要临时恢复旧链路时，可显式设置：

```text
MYPETS_ENABLE_PET_REVIEW=1
```

该开关只用于兼容和回归验证，不列入当前产品主流程。

---

## 二、数据库与部署现状

- 已有 SQLAlchemy 模型及 Alembic 环境导入；
- 尚未建立完整 `alembic/versions` 版本链；
- 开发和测试主要依赖 `Base.metadata.create_all`；
- 审核功能关闭不会删除表、对象文件或历史数据；
- 生产部署前仍需补 PostgreSQL、增量 migration、对象存储和升级回滚验证。

---

## 三、阶段状态

### 阶段 A：架构与成长基线

- [x] 服务端权威宠物模型和同步事件。
- [x] 桌面缓存、离线队列和多端当前宠物。
- [x] 五阶段成长和性格积分。
- [ ] 建立正式数据库迁移版本链。

### 阶段 B：桌面串门和多宠互动

- [x] 访客宠物独立窗口。
- [x] 宠物外出状态和主动召回。
- [x] 双宠互动及状态恢复。
- [ ] 多宠物聚会与多窗口布局。

### 阶段 C：消息与提醒

- [x] 分类消息和跨端已读。
- [x] 业务事件幂等投影。
- [x] 休眠恢复提醒摘要。
- [ ] Web 消息中心和更细提醒策略。

### 阶段 D：宠物内容审核与专属素材

- [x] D1 原图安全受理代码已实现。
- [x] D2 制作工单代码已实现。
- [x] D3 专属部署代码已实现。
- [x] 默认关闭所有 D1/D2/D3 运行入口。
- [x] 保留已发布素材只读能力。
- [ ] 当前阶段不再继续开发审核管理台、版权治理或自动素材生产。

### 阶段 E：核心养宠体验增强

- [ ] 日常任务、连续照料奖励和成长反馈。
- [ ] 多宠物批量状态查看与快捷切换。
- [ ] 更丰富的宠物动作、环境事件和主动互动。
- [ ] 好友宠物互动记录和多宠聚会。

### 阶段 F：受限 AI 聊天与语音

- [ ] 无工具调用的宠物人格文字聊天。
- [ ] 有限记忆、状态联动和安全配额。
- [ ] 按键说话、ASR/TTS、字幕和中断。
- [ ] 不提供 shell、文件、浏览器或设备控制能力。

### 阶段 G：Web 与小程序扩展

- [ ] Web 消息和提醒入口。
- [ ] 移动端登录绑定、宠物状态和互动。
- [ ] 设备管理和隐私设置。

---

## 四、近期开发顺序

1. 验证审核功能默认关闭后，现有账户、宠物、好友、串门和提醒功能不受影响。
2. 完善多宠物状态总览、快捷切换和日常养宠任务。
3. 开展多宠聚会及更丰富的双宠互动。
4. 建立正式 Alembic 迁移链和 PostgreSQL 部署验证。
5. 再进入受限 AI 文字聊天与按键语音能力。

---

## 五、验收标准

- 默认部署不显示任何宠物投稿、审核、制作或发布入口；
- 相关 API 和 `/admin` 页面默认返回 404，而不是仅在前端隐藏按钮；
- 用户门户不加载审核相关 JavaScript；
- 已发布宠物素材目录和客户端下载能力继续可用；
- 关闭功能不删除历史审核记录、工单、Release 或对象文件；
- 显式启用开关后旧功能仍能通过回归测试；
- Linux、Windows、后端、Web JavaScript 和 Qt 冒烟检查全部通过后方可合并 `main`。
