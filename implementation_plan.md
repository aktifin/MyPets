# MyPets 多人云养桌面宠物实施计划

更新基线：`main` 已合并 Web 用户门户 Phase1、内容治理安全加固、管理员治理与 D3 部署管理台、数据库增量迁移和撤销素材客户端回执闭环。本计划只记录已由代码和测试确认的能力，不再混用历史分支状态。

## 一、产品与技术边界

- 服务端是宠物状态、关系、提醒、消息、素材版本和撤销回执的权威来源。
- 桌面 SQLite 仅作为可重建缓存和断点续办队列，不作为跨端最终事实。
- 宠物素材包只能包含声明式资源和 Manifest，禁止脚本、可执行文件和工具调用。
- 投稿、制作、审核、发布、回退、版权撤销和设备清理回执必须保留角色分离、状态机和审计边界。
- AI 仅允许自然聊天、有限记忆和语音交互，不提供 Agent、Shell、文件、浏览器或设备控制能力。

## 二、当前已实现能力

| 能力域 | 状态 | 当前边界 |
|---|---|---|
| 账户、设备与同步 | 已实现 | JWT、设备凭据、快照、增量事件、多端当前宠物和设备撤销回执 |
| 多宠物与成长 | 已实现基础闭环 | 等级、经验、成长阶段、羁绊、性格和互动履历 |
| 网页与桌面照料 | 已实现 | 投喂、玩耍、清洁、摸摸、休息、冷却和日限额 |
| 好友与共同照料 | 已实现 | 好友申请、屏蔽、隐私、caregiver/viewer 邀请 |
| 异步串门 | 已实现 | 申请、接受、拒绝、取消、召回、到期返家和双宠展示 |
| 消息与已读 | 已实现 | 私聊、宠物身份、业务消息投影、分类会话和跨端已读 |
| MyReminder | 已实现基础闭环 | 提醒导入、快照、投递、完成、贪睡、忽略和恢复摘要 |
| 系统托盘与快捷操作 | 已实现 | 分组菜单、动态状态、未读消息、宠物切换、四向吸附和云端操作 |
| Web 用户门户 Phase1 | 已实现 | Dashboard、宠物、好友、串门、消息、提醒、成长、互动和部署状态 |
| 公共素材发布 | 已实现 | 模板版本、ZIP 校验、审核、不可变 Release、稳定通道和回滚 |
| 用户原图投稿 | 已实现 | 图片解码、安全清理、权利声明、审核和鉴权下载 |
| 素材制作工单 | 已实现 | 分配、进度、补图、失败重试、产物校验和日志 |
| 专属素材部署 | 已实现 Web 运营闭环 | 独立审核、私有 Release、单宠 active/previous、发布和回退 |
| 视觉身份与版权治理 | 已实现安全闭环 | 存证登记、独立复核、发布门禁、撤销分发、客户端清理和回执 |
| 管理员 RBAC | 已实现 | editor/reviewer/publisher/auditor/superadmin 页面与服务端分权 |
| 数据库迁移 | 已实现增量链 | `001_initial_schema`、`002_asset_revocation_ack`、既有库接管和双数据库 CI |
| 测试与 CI | 持续运行 | Linux、Windows、数据库、Web JavaScript 和 Qt 冒烟 |

## 三、已完成开发批次

### 内容治理安全加固

- [x] 普通账户不得调用 `/api/v1/admin/governance/*`。
- [x] 视觉身份维护归属 editor 权限。
- [x] 版权声明以 `pending` 状态登记，不再由声明人自动完成核验。
- [x] reviewer 独立复核版权存证，禁止声明人自审。
- [x] publisher 执行版权撤销并生成审计和 `asset_revoked` 同步事件。
- [x] 专属素材审核和发布必须存在最新的 `verified` 权利记录。
- [x] 版权撤销或重新待审后停止私有素材包下载。
- [x] 重复发布当前已部署 Release 按幂等读取处理，不再递增 `state_version` 或重复写审计事件。

### 数据库迁移与 PostgreSQL 基线

- [x] 建立正式 `alembic/versions` 版本链，保留兼容性初始 Revision `001_initial_schema`。
- [x] 将旧版运行时 `create_all` 基线替换为冻结的显式建表、约束、索引和降级 DDL。
- [x] 初始 Revision 纳入 35 张服务端业务表，包括视觉身份和版权治理模型。
- [x] 新增 `002_asset_revocation_ack`，验证真实的 35→36 表跨版本增量升级。
- [x] 提供历史 `Base.metadata.create_all` 数据库结构比对和安全 stamp 工具。
- [x] 完成 SQLite 的 head、单步降级、base 降级、再升级和模型漂移检查。
- [x] 完成 PostgreSQL 16 的 head、单步降级、base 降级、再升级和模型漂移检查。
- [x] 生产环境默认关闭并禁止启动自动建表。
- [x] 完成 Alembic、既有库接管、升级和回退操作文档。

### 管理台版权治理与 D3 部署闭环

- [x] 恢复与现有 `app.js` 匹配的登录、导航、模板、审核、发布和审计页面骨架。
- [x] 集成用户原图审核和素材制作工单模块。
- [x] 新增制作产物、最新版权状态、D3 审核和当前单宠部署统一工作区。
- [x] editor 可登记版权声明并提交 D3 审核。
- [x] reviewer 可独立复核存证、批准或退回专属素材。
- [x] publisher 可撤销版权、发布不可变专属 Release 并执行 active/previous 回退。
- [x] 页面展示服务端兼容性检查、权利核验、视觉身份确认和 Release 指针。
- [x] 新增管理员只读专属部署列表 API，并保持普通账户不可访问。
- [x] 管理台继续采用同源 CSP、`sessionStorage` 短期令牌和 `Cache-Control: no-store`。

### 撤销素材客户端清理与设备回执

- [x] 桌面端在推进同步游标前，将 `asset_revoked` 保存为本地持久化任务。
- [x] 清理精确版本缓存，缓存原本不存在时仍按幂等成功处理。
- [x] 写入不可逆本地撤销标记，禁止同版本再次下载、安装或热切换。
- [x] 当前宠物自动切换到内置安全兼容形象，并通过托盘状态反馈处理结果。
- [x] 清理失败任务在下次启动或同步时重试，不因断网丢失。
- [x] 设备使用短期设备令牌提交清理结果，服务端按 right/release/device 幂等更新。
- [x] 服务端校验账户关系、宠物、版权存证、产物和 Release 范围，拒绝跨账户回执。
- [x] 提供账户/设备回执查询和管理员治理回执查询接口。

### Windows 系统托盘菜单优化

- [x] 将原有平铺菜单重组为宠物互动、宠物管理、社交提醒健康、桌面行为和云端账户五个功能组。
- [x] 保留显示/隐藏宠物和未读消息作为一级快捷入口。
- [x] 菜单顶部展示当前宠物、成长等级、本地/云端归属和在家/串门状态。
- [x] 每次展开菜单时刷新窗口可见性、暂停状态、云端连接、未读消息和吸附方向。
- [x] 云端宠物与本地宠物分组展示，标识正在使用兼容形象的宠物。
- [x] 支持左、右、上、下四向吸附，以及暂时展开和解除吸附。
- [x] 根据登录状态和当前宠物范围自动启用或禁用消息、提醒、好友和串门入口。
- [x] 默认桌面启动入口切换到统一托盘菜单组合根，同时保留原有功能控制器和业务逻辑。

## 四、尚未完成的生产化工作

### A. 数据库与部署

- [ ] 完成对象存储生产适配、备份恢复演练和发布回滚演练。
- [ ] 增加生产部署模板、迁移前检查和维护窗口脚本。
- [ ] 为新增 Revision 建立生产数据量级压测和长事务监控。

### B. 内容运营闭环

- [ ] 为版权存证增加附件证据、有效期、复核意见和历史版本展示。
- [ ] 在管理台增加设备清理回执聚合、失败设备筛选和人工跟进状态。
- [ ] 对视觉特征一致性引入可解释评分，但不得自动替代人工审核。
- [ ] 增加批量筛选、批量复核和运营统计，但保留逐项人工确认。

### C. 核心养宠体验

- [ ] 日常任务、连续照料奖励和长期成长平衡。
- [ ] 多宠批量状态查看、批量切换和统一提醒。
- [ ] 多宠物聚会模型、邀请状态机和多窗口布局。
- [ ] 更丰富的环境事件、主动互动和好友宠物互动记录。

### D. 受限 AI 交互

- [ ] 无工具调用的宠物人格文字聊天。
- [ ] 有限记忆、用户可清除记忆和状态联动。
- [ ] 按键说话、ASR/TTS、字幕和中断控制。
- [ ] 明确禁止常开监听、工具执行和设备控制。

## 五、下一批优先级

1. 为版权存证增加附件证据、有效期、复核意见和历史版本。
2. 在管理台展示设备清理回执聚合和失败跟进状态。
3. 完成对象存储生产适配与备份恢复演练方案。
4. 开始多宠物聚会服务端模型与状态机。

## 六、完成判定

任何功能只有同时满足以下条件才标记为完成：

- 服务端状态机或客户端逻辑已实现；
- 权限、幂等、审计和异常路径已覆盖；
- SQLite 与 PostgreSQL 迁移 CI 通过；
- Linux 与 Windows CI 通过；
- Web JavaScript、后端编译和 Qt 冒烟通过；
- 文档描述与 `main` 中实际代码一致。
